# ----------------------------------------------------------------------------
# Private AI Platform — API configuration
# Copy to .env and adjust. All values have safe local defaults.
# ----------------------------------------------------------------------------

# Core
APP_ENV=development
SECRET_KEY=change-me-in-production-please-32-chars-min
ACCESS_TOKEN_EXPIRE_MINUTES=720

# Database. Default = local SQLite file. For Supabase/Postgres use the pooled
# connection string from Supabase (Project Settings > Database):
# DATABASE_URL=postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DATABASE_URL=sqlite:///./privateai.db

# CORS (comma separated origins for the web portal)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ---- Privacy Model Router -------------------------------------------------
# Routes are tried in order of the policy. The MOCK adapter always works with
# zero configuration so the platform runs end-to-end out of the box.

# Premium external model (OpenAI / Claude / Gemini compatible endpoint)
PREMIUM_ENABLED=false
PREMIUM_BASE_URL=https://api.openai.com/v1
PREMIUM_API_KEY=
PREMIUM_MODEL=gpt-4o-mini

# Cost-optimized open-models / volume route — provider: NaN Builders.
# OpenAI-compatible endpoint; set the base URL + key NaN Builders gives you.
OPEN_ENABLED=false
OPEN_PROVIDER_NAME=NaN Builders
OPEN_BASE_URL=https://api.nanbuilders.com/v1
OPEN_API_KEY=
OPEN_MODEL=meta-llama/llama-3.1-8b-instruct

# VPC / privately hosted OpenAI-compatible endpoint (vLLM / TGI)
VPC_ENABLED=false
VPC_BASE_URL=http://vllm:8000/v1
VPC_API_KEY=
VPC_MODEL=Qwen2.5-7B-Instruct

# Local / self-hosted (Ollama)
LOCAL_ENABLED=false
LOCAL_BASE_URL=http://localhost:11434/v1
LOCAL_MODEL=llama3.1

# ---- Embeddings / RAG -----------------------------------------------------
# Default = deterministic local embedder (no external calls).
# EMBEDDINGS_PROVIDER: local | open (NaN Builders, reuses OPEN_BASE_URL/OPEN_API_KEY)
#                      | premium (reuses PREMIUM_BASE_URL/PREMIUM_API_KEY)
EMBEDDINGS_PROVIDER=local
EMBEDDINGS_MODEL=text-embedding-3-small
EMBEDDINGS_DIM=384

# Vector store: "inprocess" (default) | "pgvector" (Supabase/Postgres) | "qdrant".
# pgvector reuses DATABASE_URL (run infra/supabase/001_pgvector.sql once).
# qdrant: pip install qdrant-client and run the Qdrant service.
VECTOR_STORE=inprocess
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# ---- Enterprise hardening (Fase 5) ----------------------------------------
# Encryption at rest (AES-256-GCM). MASTER_KMS_KEY should come from a real KMS
# (AWS KMS / GCP KMS / Vault) in production. Bump KMS_KEY_VERSION to rotate.
ENCRYPTION_ENABLED=true
MASTER_KMS_KEY=
KMS_KEY_VERSION=1

# Provider resilience: privacy-safe fallback order when a route's adapter fails.
FALLBACK_ORDER=vpc,open,local

# SSO / OIDC (optional). When enabled, password login is replaced by OIDC.
SSO_ENABLED=false
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=http://localhost:3000/auth/callback
OIDC_DEFAULT_ROLE=user

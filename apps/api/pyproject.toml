[project]
name = "private-ai-platform-api"
version = "0.1.0"
description = "MaestroAI — Private AI Gateway + Vertical Agents (backend API, FastAPI)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

{
  "name": "private-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Private AI Gateway + Vertical Agents — plataforma de AI privada para México.",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @private-ai/web",
    "build:web": "npm run build --workspace @private-ai/web"
  }
}
